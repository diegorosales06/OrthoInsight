numpy
PyQt6
pyqtgraph
PyYAML

# Hardware-only (Raspberry Pi) — not needed for --debug mode:
# spidev
# gpiozero
# lgpio

# Arch-mesh bake only (laptop, tools/bake_arch_mesh.py) — the Pi runtime reads
# the baked asset with stdlib alone and must never import these:
trimesh
